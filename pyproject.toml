[project]
name = "job-search"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.15"
dependencies = [
    "flask>=3.0",
    "flask-cors>=4.0",
    "typer>=0.12",
    "rich>=13",
    "websockets>=12.0",
]

[project.scripts]
jobsearch = "app.server.cli:app"
