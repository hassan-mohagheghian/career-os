FROM python:3.14-slim

WORKDIR /app

COPY pyproject.toml uv.lock ./
COPY app/ ./app/

RUN pip install uv && uv sync --frozen --no-dev

ENV PYTHONPATH=/app

CMD ["uv", "run", "python", "-m", "background.main"]
