FROM python:3.14-slim

WORKDIR /app

COPY pyproject.toml uv.lock ./
COPY apps/ ./apps/

RUN pip install uv && uv sync --frozen --no-dev

ENV PYTHONPATH=/app:/app/apps/backend

CMD ["uv", "run", "python", "-m", "apps.backend.entrypoints.worker"]
