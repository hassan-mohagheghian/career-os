Extend this {skill_name} roadmap. Add advanced items and fill coverage gaps.

EXISTING TREE:
{existing_tree_json}

User has completed: {checked_items}

RULES:
1. Keep ALL existing items unchanged (same title, description, level, children)
2. Add NEW items that go beyond the current level range (fill 800-1000 if not covered)
3. Fill any important coverage gaps between existing levels
4. The result must be a SUPERSET of the original
5. 15-20 root items total, each with 2-4 children
6. Max 2 levels of nesting
7. ONLY items about {skill_name} — nothing else
8. Levels 000-1000, ordered ascending

OUTPUT: Reply with ONLY a valid JSON array containing the COMPLETE roadmap (existing + new). No markdown, no explanation, no code fences.

[
  {{
    "title": "Specific Item Title",
    "description": "What this covers and what interview question it answers",
    "level": 250,
    "children": []
  }}
]
